/* eslint-disable no-unused-vars */
import React from "react";
import { styled } from "styled-components";

const CheckIconWrapper = styled.span`
  color: #0d47a1;
  margin-right: 10px;
`;

const CheckIcon = () => {
  return <CheckIconWrapper>✔</CheckIconWrapper>;
};

export default CheckIcon;
